import { describe,expect,it,vi } from "vitest";import ExcelJS from "exceljs";import PDFDocument from "pdfkit";import { requerimientoViaticoExcel,requerimientoViaticoPdf } from "./viaticos-requerimientos-export";
const d:Parameters<typeof requerimientoViaticoExcel>[0]={id:1,codigo:"VR-2026-000001",estado:"AUTORIZADO",total:"200.50",fecha_requerimiento:"2026-09-21",empresa_requirente_nombre:"KuiqTrans",requirente_nombre_snapshot:"Ana",solicitante_nombre_snapshot:"Luis",autorizado_por_nombre:"Carlos",observaciones:"Prueba",lineas:[{id:2,fecha_solicitud:"2026-09-22",fecha_viaje:"2026-09-24",personal_nombre_snapshot:"Piloto Uno",cargo_snapshot:"Piloto",placa_snapshot:"C-123ABC",cliente_nombre_snapshot:"Cliente",destino:"Escuintla",cantidad:"2",monto_unitario:"100.25",total:"200.50"}]};
describe("exportaciones de requerimiento de viáticos",()=>{
 it("Excel muestra la empresa seleccionada y conserva fechas, columnas, datos y total",async()=>{const bytes=await requerimientoViaticoExcel(d);const wb=new ExcelJS.Workbook();await wb.xlsx.load(new Uint8Array(bytes).buffer);const ws=wb.worksheets[0];expect(ws.getCell("A1").value).toBe("KuiqTrans");expect(ws.getCell("A2").value).toBe("REQUERIMIENTO DE VIÁTICOS");expect(ws.getRow(5).values).toEqual(expect.arrayContaining(["Fecha solicitud","Fecha viaje","Colaborador","Monto","Total"]));expect(ws.getRow(6).values).toEqual(expect.arrayContaining(["2026-09-22","2026-09-24"]));expect(ws.getColumn(11).values).toContain(200.5);});
 it("PDF Letter horizontal muestra la empresa seleccionada, ambas fechas y firmas",async()=>{const text=vi.spyOn(PDFDocument.prototype,"text");const b=await requerimientoViaticoPdf({...d,empresa_requirente_nombre:"Logiservicios Mónaco"});const valores=text.mock.calls.map(c=>String(c[0]));expect(valores).toEqual(expect.arrayContaining(["Logiservicios Mónaco","Fecha solicitud","Fecha viaje","2026-09-22","2026-09-24","FIRMA DE LA PERSONA QUE REQUIERE","AUTORIZADO POR"]));expect(valores).not.toContain("Kuiqtrans / Logiservicios Mónaco");expect(b.subarray(0,4).toString()).toBe("%PDF");expect(b.length).toBeGreaterThan(1000);text.mockRestore();});
});
