import { describe, expect, it } from "vitest";
import { guardarRequerimientoViaticoSchema, transicionRequerimientoViaticoSchema } from "./viaticos-requerimientos-schema";
const linea={fecha:"2026-09-21",personalId:3,vehiculoId:4,clienteId:5,cantidad:"2",destino:"Escuintla",montoUnitario:"100.25",motivoCambio:"Ruta especial",observaciones:null};
describe("requerimientos manuales de viáticos",()=>{
  it("acepta captura manual estricta y rechaza snapshots/plan falsificados",()=>{const base={fechaRequerimiento:"2026-09-21",requirenteUsuarioId:8,estado:"PENDIENTE",lineas:[linea]};expect(guardarRequerimientoViaticoSchema.safeParse(base).success).toBe(true);expect(guardarRequerimientoViaticoSchema.safeParse({...base,empresaRequirenteNombre:"falso"}).success).toBe(false);expect(guardarRequerimientoViaticoSchema.safeParse({...base,lineas:[{...linea,planId:9}]}).success).toBe(false);});
  it("cantidad debe ser positiva y montos tienen dos decimales",()=>{const base={fechaRequerimiento:"2026-09-21",requirenteUsuarioId:8,lineas:[linea]};expect(guardarRequerimientoViaticoSchema.safeParse({...base,lineas:[{...linea,cantidad:"0"}]}).success).toBe(false);expect(guardarRequerimientoViaticoSchema.safeParse({...base,lineas:[{...linea,montoUnitario:"1.234"}]}).success).toBe(false);});
  it.each(["enviar","autorizar","rechazar","entregar","liquidar"])("valida payload de transición %s",accion=>{const body:any={accion,version:1};if(accion==="rechazar")body.motivo="Dato incorrecto";if(accion==="entregar")Object.assign(body,{metodo:"EFECTIVO",referencia:null,observaciones:null});if(accion==="liquidar")body.observaciones=null;expect(transicionRequerimientoViaticoSchema.safeParse(body).success).toBe(true);});
});
